apiVersion: v1
kind: Service
metadata:
  name: kanchi_example
  namespace: {{YOUR_K8S_NAMESPACE}}
spec:
  selector:
    app: kanchi_example
  ports:
    - name: api
      port: 8765
      targetPort: 8765
    - name: ui
      port: 3000
      targetPort: 3000
  type: ClusterIP
